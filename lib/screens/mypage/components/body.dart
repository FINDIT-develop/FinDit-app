import 'package:flutter/material.dart';
import 'package:moic/screens/mypage/components/profile_menu.dart';
import 'package:moic/screens/mypage/components/profile_pic.dart';

class Body extends StatelessWidget {
  const Body({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        // ProfilePic(),
        // ProfileMenu(),
      ],
    );
  }
}
